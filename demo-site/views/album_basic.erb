<% form(Album[1], {:action=>'/album', :method=>:get}, form_opts) do |f| %>
  <%= f.inputs([:name, [:artist, form_opts[:one]||{}], :release_date, :copies_sold, :debut_album, :out_of_print, [:tracks, form_opts[:many]||{}], [:tags, form_opts[:many]||{}]], :legend=>'Album') %>
  <%= f.button(:value=>'Update Album') %>
<% end %>
